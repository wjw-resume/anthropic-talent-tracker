import { MaxLengthPipePipe } from './max-length-pipe.pipe';

describe('MaxLengthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MaxLengthPipePipe();
    expect(pipe).toBeTruthy();
  });
});
