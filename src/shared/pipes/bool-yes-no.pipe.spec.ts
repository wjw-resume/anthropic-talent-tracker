import { BoolYesNoPipe } from './bool-yes-no.pipe';

describe('BoolYesNoPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolYesNoPipe();
    expect(pipe).toBeTruthy();
  });
});
